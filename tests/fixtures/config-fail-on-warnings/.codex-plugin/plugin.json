{
  "name": "config-fail-on-warnings",
  "version": "1.0.0",
  "description": "This plugin description is intentionally far too long for the manifest-level metadata heuristic because this fixture verifies that local policy configuration can promote warnings into a blocking validation result for stricter CI workflows. It keeps going with extra wording so the package-level description length crosses the warning threshold deterministically during tests."
}
